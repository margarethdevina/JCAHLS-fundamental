// array of object
/**
 * arrVar = [
 * {
 * prop1: val1,
 * prop2: val2
 * },
 * {
 * }
 * ]
 * 
 * output2 dr database biasanya seperti ini
 * biasanya dipakai dalam json / javascript object notation
 * 
 * fungsi yg sering dipk
 * arrVar.map ((v,i)=>{program, return})
 * arrVar.forEach ((v,i)=>{program})
 * arrVar.filter((v,i)=>{program,return})
 * arrVar.findIndex(v=>v.prop == val)
 * 
 * 
 */